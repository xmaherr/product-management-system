package dao;


import entity.Product;


public class ProductDaoImpl implements ProductDao {
    @Override
    public void addProduct(Product product) {
//       product SessionFactory sessionFactory = new Configuration().configure().buildSessionFactory();
    }
}
